# ✅ Исправление рассадки и списка игроков

## 🐛 Проблемы

### Проблема 1: Рассадка не показывается пользователю
**Симптомы:**
- Рассадка сгенерирована в админ панели
- Пользователь не видит свое место во вкладке "Посадка"
- Должно показываться после начала турнира

**Причина:**
- `SeatingView` пытался получить `table_number` и `seat_number` из `game_registrations`
- Но эти колонки не существуют в `game_registrations`
- Данные о рассадке хранятся в `table_assignments`

### Проблема 2: Игроки не видны в разделе "Турниры"
**Симптомы:**
- После начала турнира в разделе "Турниры" показывается "нет зарегистрированных игроков"
- Хотя игроки есть и участвуют в турнире
- Должны показываться все игроки, которые участвуют (paid/playing)

**Причина:**
- `getRegisteredUsers` возвращал только игроков со статусом 'registered'
- После начала турнира статус игроков меняется на 'paid' или 'playing'
- Нужно показывать игроков с этими статусами для начатого турнира

---

## ✅ Исправления

### 1. Исправлен `SeatingView` - использование правильного API

**Файл:** `client/src/components/SeatingView.tsx`

**Было:**
```typescript
// Загружаем регистрации для этой игры
const registrations = await gamesAPI.getRegistrations(game.id);
const myRegistration = registrations.find((r: any) => r.user_id === user.id);

if (myRegistration && myRegistration.status === 'paid' && myRegistration.table_number) {
  setMySeating({
    table_number: myRegistration.table_number,
    seat_number: myRegistration.seat_number,
  });
}
```

**Стало:**
```typescript
// Загружаем рассадку для этой игры из table_assignments
const seating = await gamesAPI.getSeating(game.id);
const mySeat = seating.find((s: any) => s.user_id === user.id);

if (mySeat) {
  setMySeating({
    table_number: mySeat.table_number,
    seat_number: mySeat.seat_number,
  });
}
```

**Результат:**
- ✅ Используется правильный API метод `getSeating()`
- ✅ Данные берутся из `table_assignments`
- ✅ Пользователь видит свое место после начала турнира

---

### 2. Исправлен `getRegisteredUsers` - показ игроков для начатого турнира

**Файл:** `server/models/Game.js`

**Было:**
```javascript
async getRegisteredUsers(gameId) {
  const result = await query(
    `SELECT u.id, u.telegram_id, u.username, u.first_name, u.last_name,
            u.photo_url, gr.registered_at, gr.status
     FROM game_registrations gr
     JOIN users u ON u.id = gr.user_id
     WHERE gr.game_id = $1 AND gr.status = 'registered'
     ORDER BY gr.registered_at`,
    [gameId]
  );
  
  return result.rows;
}
```

**Стало:**
```javascript
async getRegisteredUsers(gameId) {
  // Сначала проверяем статус турнира
  const game = await this.getById(gameId);
  const isStarted = game.tournament_status === 'started';
  
  // Для начатого турнира показываем игроков, которые участвуют (paid/playing)
  // Для ожидающего турнира показываем зарегистрированных (registered)
  let sql;
  let params;
  
  if (isStarted) {
    sql = `SELECT u.id, u.telegram_id, u.username, u.first_name, u.last_name,
                  u.photo_url, gr.registered_at, gr.status
           FROM game_registrations gr
           JOIN users u ON u.id = gr.user_id
           WHERE gr.game_id = $1 AND gr.status IN ('paid', 'playing')
           ORDER BY gr.registered_at`;
    params = [gameId];
  } else {
    sql = `SELECT u.id, u.telegram_id, u.username, u.first_name, u.last_name,
                  u.photo_url, gr.registered_at, gr.status
           FROM game_registrations gr
           JOIN users u ON u.id = gr.user_id
           WHERE gr.game_id = $1 AND gr.status = 'registered'
           ORDER BY gr.registered_at`;
    params = [gameId];
  }
  
  const result = await query(sql, params);
  return result.rows;
}
```

**Результат:**
- ✅ Для начатого турнира показываются игроки со статусом 'paid' или 'playing'
- ✅ Для ожидающего турнира показываются игроки со статусом 'registered'
- ✅ Пользователи видят всех участников начатого турнира

---

### 3. Исправлен `getUserGames` - правильная фильтрация по статусу турнира

**Файл:** `server/models/Game.js`

**Было:**
```javascript
if (status) {
  sql += ' AND gr.status = $2';
  params.push(status);
}
```

**Стало:**
```javascript
if (status) {
  // Если статус 'started', фильтруем по tournament_status
  // Иначе фильтруем по статусу регистрации
  if (status === 'started') {
    sql += ' AND g.tournament_status = $2';
    params.push('started');
  } else {
    sql += ' AND gr.status = $2';
    params.push(status);
  }
}
```

**Результат:**
- ✅ При запросе `getUserGames('started')` фильтруется по `tournament_status`
- ✅ Пользователь видит начатые турниры, в которых участвует
- ✅ Рассадка загружается для правильных турниров

---

## 📊 Логика работы

### Рассадка для пользователя:

1. **Пользователь открывает вкладку "Посадка"**
   - ✅ Загружаются турниры со статусом 'started', в которых участвует пользователь
   - ✅ Для каждого турнира загружается рассадка из `table_assignments`
   - ✅ Находится место пользователя по `user_id`

2. **Отображение рассадки:**
   - ✅ Показывается номер стола и место
   - ✅ Отображается визуализация стола
   - ✅ Подсвечивается место пользователя

### Список игроков в разделе "Турниры":

1. **Для ожидающего турнира:**
   - ✅ Показываются игроки со статусом 'registered'
   - ✅ Это игроки, которые зарегистрировались, но еще не оплатили

2. **Для начатого турнира:**
   - ✅ Показываются игроки со статусом 'paid' или 'playing'
   - ✅ Это игроки, которые оплатили и участвуют в турнире
   - ✅ Исключаются игроки со статусом 'no_show'

---

## 🧪 Тестирование

### Рассадка:

1. ✅ Администратор начинает турнир
2. ✅ Пользователь открывает вкладку "Посадка"
3. ✅ Должна отображаться рассадка с номером стола и места
4. ✅ Визуализация стола должна показывать место пользователя

**Ожидаемый результат:**
- ✅ Пользователь видит "Стол X, Место Y"
- ✅ Визуализация стола показывает его место
- ✅ Информация совпадает с админ панелью

### Список игроков:

1. ✅ Администратор начинает турнир
2. ✅ Пользователь открывает раздел "Турниры"
3. ✅ Нажимает на турнир
4. ✅ Открывается список участников

**Ожидаемый результат:**
- ✅ Показываются все игроки, которые участвуют (paid/playing)
- ✅ Не показывается "нет зарегистрированных игроков"
- ✅ Список совпадает с админ панелью

---

## 📁 Измененные файлы

1. ✅ `client/src/components/SeatingView.tsx`
   - Использует `getSeating()` вместо `getRegistrations()`
   - Получает рассадку из `table_assignments`

2. ✅ `server/models/Game.js`
   - `getRegisteredUsers()` - показывает игроков в зависимости от статуса турнира
   - `getUserGames()` - правильно фильтрует по `tournament_status` для статуса 'started'

---

## 🎯 Статус: ГОТОВО!

✅ Рассадка показывается пользователю после начала турнира  
✅ Список игроков показывается для начатого турнира  
✅ Данные совпадают с админ панелью  
✅ Правильная работа с `table_assignments`  

---

## 📝 Примечания

- Данные о рассадке всегда хранятся в `table_assignments`, а не в `game_registrations`
- Для начатого турнира нужно показывать игроков со статусом 'paid' или 'playing'
- Для ожидающего турнира показываются игроки со статусом 'registered'

---

**После деплоя пользователи увидят рассадку и список игроков! 🎉**

