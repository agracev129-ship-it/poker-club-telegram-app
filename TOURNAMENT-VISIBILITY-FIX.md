# ✅ Исправление: турниры исчезают после начала

## 🐛 Проблема

**Симптомы:**
1. Когда администратор начинает турнир, он пропадает у всех игроков
2. Турнир пропадает из админ панели при обновлении страницы
3. После начала турнира его статус меняется с 'upcoming' на 'started', и он исчезает из списка

**Причина:**
- Фильтр на фронтенде запрашивает только турниры со статусом 'upcoming'
- После начала турнира статус меняется на 'started'
- Турнир больше не попадает в фильтр и исчезает из списка

---

## ✅ Исправление

### Изменена логика фильтрации на бэкенде

**Файл:** `server/models/Game.js`

**Метод:** `getAll()`

**Было:**
```javascript
if (filters.status && filters.status !== 'all') {
  conditions.push(`(g.tournament_status = $${paramCount} OR (g.tournament_status IS NULL AND g.status = $${paramCount}))`);
  params.push(filters.status);
  paramCount++;
}
```

**Стало:**
```javascript
if (filters.status && filters.status !== 'all') {
  // ВАЖНО: 'upcoming' включает и 'started' турниры, чтобы они не исчезали после начала
  if (filters.status === 'upcoming') {
    conditions.push(`(g.tournament_status = 'upcoming' OR g.tournament_status = 'started' OR (g.tournament_status IS NULL AND g.status = 'upcoming'))`);
  } else {
    // Для других статусов используем обычную фильтрацию
    conditions.push(`(g.tournament_status = $${paramCount} OR (g.tournament_status IS NULL AND g.status = $${paramCount}))`);
    params.push(filters.status);
    paramCount++;
  }
}
```

**Результат:**
- ✅ При запросе турниров со статусом 'upcoming' возвращаются и 'upcoming', и 'started' турниры
- ✅ Турниры не исчезают после начала
- ✅ Игроки видят активные турниры (ожидающие и идущие)
- ✅ Администратор видит все активные турниры в панели управления

---

## 📊 Логика работы

### Статусы турниров:

1. **'upcoming'** - Турнир ожидает начала
   - Регистрация открыта
   - Игроки могут регистрироваться
   - Показывается в списке "Предстоящие"

2. **'started'** - Турнир начался
   - Регистрация закрыта
   - Игроки играют
   - Рассадка сгенерирована
   - **Теперь показывается вместе с 'upcoming'**

3. **'completed'** - Турнир завершен
   - Результаты подведены
   - Очки начислены
   - Показывается в истории

### Фильтрация:

- **Запрос `{ status: 'upcoming' }`** → возвращает турниры со статусом 'upcoming' **И** 'started'
- **Запрос `{ status: 'started' }`** → возвращает только турниры со статусом 'started'
- **Запрос `{ status: 'completed' }`** → возвращает только завершенные турниры
- **Запрос `{ status: 'all' }`** → возвращает все турниры

---

## 🧪 Тестирование

### Для игроков:

1. ✅ Откройте главную страницу приложения
2. ✅ Убедитесь, что видите турнир со статусом "Ожидание"
3. ✅ Администратор начинает турнир
4. ✅ Обновите страницу (или подождите автоматического обновления)

**Ожидаемый результат:**
- ✅ Турнир **НЕ исчезает** из списка
- ✅ Статус меняется на "В процессе" (зеленый badge)
- ✅ Турнир виден в списке активных турниров
- ✅ Можно открыть рассадку и увидеть свой стол и место

### Для администратора:

1. ✅ Откройте панель управления турнирами
2. ✅ Найдите турнир со статусом "Ожидание"
3. ✅ Нажмите "Начать турнир"
4. ✅ Обновите страницу

**Ожидаемый результат:**
- ✅ Турнир **НЕ исчезает** из списка
- ✅ Статус меняется на "Идёт" (зеленый badge)
- ✅ Турнир виден в панели управления
- ✅ Можно управлять турниром (исключать игроков, завершать и т.д.)

---

## 📁 Измененные файлы

1. ✅ `server/models/Game.js`
   - `getAll()` - изменена логика фильтрации для статуса 'upcoming'
   - Теперь 'upcoming' включает и 'started' турниры

---

## 🎯 Статус: ГОТОВО!

✅ Турниры не исчезают после начала  
✅ Игроки видят активные турниры  
✅ Администратор видит все активные турниры  
✅ Логика фильтрации работает корректно  

---

## 📝 Примечания

- Фильтр 'upcoming' теперь включает 'started' турниры, так как это логично - пользователи должны видеть активные турниры
- Для других статусов фильтрация работает как обычно
- Это изменение не влияет на другие части системы

---

**После деплоя турниры будут оставаться видимыми после начала! 🎉**

