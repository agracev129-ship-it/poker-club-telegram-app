# 🧪 ПОЛНОЕ РУКОВОДСТВО ПО ТЕСТИРОВАНИЮ ЖИЗНЕННОГО ЦИКЛА ТУРНИРА

## 📋 СОДЕРЖАНИЕ
1. Подготовка к тестированию
2. Тестирование полного жизненного цикла
3. Проверка автоматизации
4. Bug Testing
5. Чек-лист функционала

---

## 🚀 1. ПОДГОТОВКА К ТЕСТИРОВАНИЮ

### Шаг 1: Установка зависимостей

```bash
# Backend
cd server
npm install node-cron
cd ..

# Проверка установки
node -e "console.log(require('node-cron'))"
```

### Шаг 2: Миграция базы данных

```bash
cd server
# Запустить сервер - миграция выполнится автоматически
npm start
```

Ожидаемый вывод:
```
🎰 Initializing Tournament Lifecycle System...
✅ Tournament Lifecycle System initialized successfully!
✅ Tournament automation jobs initialized
📋 Active jobs:
  • Auto-open registration (every hour)
  • Auto-start check-in (every 15 min)
  ...
```

### Шаг 3: Проверка структуры БД

Подключитесь к PostgreSQL и проверьте:

```sql
-- Проверка новых таблиц
SELECT table_name FROM information_schema.tables 
WHERE table_schema = 'public' 
AND table_name IN ('tournament_payments', 'tournament_actions_log', 'tournament_point_structure');

-- Должно вернуть 3 таблицы

-- Проверка новых полей в games
SELECT column_name FROM information_schema.columns 
WHERE table_name = 'games' 
AND column_name IN ('tournament_status', 'check_in_opens_at', 'total_prize_pool');

-- Должно вернуть 3 поля

-- Проверка новых полей в game_registrations
SELECT column_name FROM information_schema.columns 
WHERE table_name = 'game_registrations' 
AND column_name IN ('payment_status', 'payment_amount', 'checked_in_at', 'finish_position');

-- Должно вернуть 4+ полей
```

---

## 🎮 2. ТЕСТИРОВАНИЕ ПОЛНОГО ЖИЗНЕННОГО ЦИКЛА

### Сценарий: Идеальный турнир от начала до конца

#### Этап 1: Создание турнира ✅

1. Войдите как администратор
2. Перейдите в админ-панель → Турниры
3. Нажмите "Создать турнир"
4. Заполните данные:
   - Название: "TEST TOURNAMENT"
   - Дата: Сегодня + 1 день
   - Время: Через 2 часа
   - Max игроков: 20
   - Buy-in: 1000₽
5. Настройте структуру очков (оставьте стандартную)
6. Создайте турнир

**Проверка:**
- Турнир создан
- Статус: `upcoming`
- В БД появилась запись в `games`
- В БД появилась запись в `tournament_point_structure`

```sql
SELECT * FROM games WHERE name = 'TEST TOURNAMENT';
SELECT * FROM tournament_point_structure WHERE game_id = [ID турнира];
```

#### Этап 2: Открытие регистрации 📝

1. Откройте управление турниром
2. Нажмите "Открыть регистрацию"

**Проверка:**
- Статус турнира изменился на `registration_open`
- В `tournament_actions_log` появилась запись `open_registration`

```sql
SELECT tournament_status FROM games WHERE id = [ID];
SELECT * FROM tournament_actions_log WHERE game_id = [ID] AND action_type = 'open_registration';
```

#### Этап 3: Регистрация игроков 👥

1. Выйдите из админ-режима
2. Зарегистрируйтесь на турнир как обычный пользователь
3. Повторите для 10-15 пользователей (можно создать тестовых)

**Проверка:**
- В `game_registrations` появились записи
- Статус каждой регистрации: `registered`
- `payment_status`: `pending`

```sql
SELECT COUNT(*) FROM game_registrations WHERE game_id = [ID];
SELECT status, payment_status FROM game_registrations WHERE game_id = [ID];
```

#### Этап 4: Начало приема игроков 🎫

1. Вернитесь в админ-режим
2. Откройте управление турниром
3. Нажмите "Начать прием игроков"
4. Откроется AdminCheckInView

**Проверка:**
- Статус турнира: `check_in`
- Отображается список зарегистрированных игроков
- Статистика: "Всего: 15, Явились: 0, Оплатили: 0"

#### Этап 5: Отметка явки и оплаты 💰

1. В AdminCheckInView найдите игрока
2. Нажмите кнопку "✓" (отметить явку)
3. Статус игрока изменится на `checked_in`
4. Нажмите кнопку "$" (подтвердить оплату)
5. Заполните форму:
   - Сумма: 1000
   - Способ: Наличные
6. Подтвердите

**Проверка:**
- Статус игрока: `paid`
- `payment_status`: `paid`
- В `tournament_payments` создана запись
- Статистика обновилась

```sql
SELECT status, payment_status, payment_amount 
FROM game_registrations 
WHERE game_id = [ID] AND user_id = [USER_ID];

SELECT * FROM tournament_payments 
WHERE game_id = [ID] AND user_id = [USER_ID];

SELECT * FROM tournament_actions_log 
WHERE game_id = [ID] 
AND action_type IN ('check_in_player', 'confirm_payment');
```

7. Повторите для 8-10 игроков
8. Оставьте 2-3 игрока без отметки (для теста no-show)

#### Этап 6: Регистрация на месте 🏃

1. В AdminCheckInView нажмите "Добавить игрока"
2. Введите ID нового игрока
3. Укажите сумму оплаты
4. Подтвердите

**Проверка:**
- Игрок добавлен в список
- `registration_type`: `onsite`
- Статус сразу: `paid`
- Автоматически отмечены `checked_in_at` и `paid_at`

#### Этап 7: Исключение неявившихся ❌

1. В AdminCheckInView нажмите "Исключить неявившихся"
2. Подтвердите действие

**Проверка:**
- Все игроки со статусом `registered` изменены на `no_show`
- Для каждого в `tournament_actions_log` запись `mark_no_show`
- Статистика обновилась

```sql
SELECT COUNT(*) FROM game_registrations 
WHERE game_id = [ID] AND status = 'no_show';
```

#### Этап 8: Начало турнира 🎲

1. Вернитесь в AdminTournamentManagementView
2. Нажмите "Начать турнир"
3. Подождите генерации рассадки

**Проверка:**
- Статус турнира: `started`
- В `table_assignments` созданы записи рассадки
- Игроки равномерно распределены по столам (по 9-10 за столом)
- Каждому игроку назначены `table_number` и `seat_number`

```sql
SELECT tournament_status FROM games WHERE id = [ID];

SELECT table_number, COUNT(*) as players_count 
FROM table_assignments 
WHERE game_id = [ID] 
GROUP BY table_number;
```

#### Этап 9: Поздняя регистрация ⏰

1. Во время турнира нажмите "Поздняя регистрация"
2. Откроется AdminLateRegistrationView
3. Введите ID опоздавшего игрока
4. Выберите свободное место за столом
5. Укажите сумму оплаты и начальный стек (меньше стандартного)
6. Подтвердите

**Проверка:**
- Игрок добавлен в `game_registrations`
- Статус: `late_registered`
- `is_late_entry`: `true`
- Игрок добавлен в `table_assignments`
- В `tournament_payments` создана запись

```sql
SELECT * FROM game_registrations 
WHERE game_id = [ID] AND registration_type = 'late';

SELECT * FROM table_assignments 
WHERE game_id = [ID] AND user_id = [LATE_USER_ID];
```

#### Этап 10: Выбывание игроков 🏆

1. В списке активных игроков нажмите "Выбить" на игроке
2. Система автоматически рассчитает место
3. Повторите пока не останется 3-5 игроков

**Проверка:**
- У выбывших игроков `is_eliminated`: `true`
- Назначено `finish_place`
- Рассчитаны `points_earned` по структуре
- В `tournament_actions_log` записи `eliminate_player`

```sql
SELECT user_id, finish_place, points_earned, is_eliminated 
FROM table_assignments 
WHERE game_id = [ID] 
ORDER BY finish_place;
```

#### Этап 11: Ребалансировка столов 🔄

1. Когда игроков мало, нажмите "Ребалансировать столы"
2. Система автоматически пересадит игроков

**Проверка:**
- Игроки распределены оптимально
- Все столы заполнены равномерно
- В `tournament_actions_log` запись `rebalance_tables`

#### Этап 12: Завершение турнира 🏁

1. Когда остался 1 игрок, нажмите "Завершить турнир"
2. Подтвердите действие

**Проверка:**
- Статус турнира: `completed`
- У всех игроков начислены очки
- Обновлена статистика в `user_stats`
- Обновлен рейтинг (вызван `User.updateRankings()`)
- В `user_activities` записи о победе/участии

```sql
SELECT tournament_status, finished_at FROM games WHERE id = [ID];

SELECT u.name, us.total_points, us.games_played, us.current_rank
FROM users u
JOIN user_stats us ON u.id = us.user_id
WHERE u.id IN (
  SELECT user_id FROM game_registrations WHERE game_id = [ID]
)
ORDER BY us.total_points DESC;
```

---

## 🤖 3. ТЕСТИРОВАНИЕ АВТОМАТИЗАЦИИ

### Test 1: Автооткрытие регистрации

1. Создайте турнир на дату через 6 дней
2. Подождите выполнения cron job (каждый час)
3. Или вручную вызовите:

```javascript
// В Node.js REPL или отдельном скрипте
import { query } from './server/database/db.js';
// Выполните код из cron job для тестирования
```

**Проверка:** Статус изменился на `registration_open`

### Test 2: Автоначало check-in

1. Создайте турнир на сегодня, время через 55 минут
2. Подождите выполнения cron job (каждые 15 минут)

**Проверка:** Статус изменился на `check_in`

### Test 3: Автоисключение неявившихся

1. Создайте турнир со статусом `check_in`
2. Зарегистрируйте нескольких игроков
3. Не отмечайте их явку
4. Установите время турнира на "через 8 минут"
5. Подождите выполнения cron job (каждые 5 минут)

**Проверка:** Неявившиеся отмечены как `no_show`

### Test 4: Обновление призовых фондов

1. Создайте турнир
2. Подтвердите несколько оплат
3. Подождите выполнения cron job (каждый час)

**Проверка:** Поле `total_prize_pool` обновлено

---

## 🐛 4. BUG TESTING

### Bug Test 1: Duplicate Payment

**Сценарий:**
1. Отметьте явку игрока
2. Подтвердите оплату
3. Попробуйте подтвердить оплату еще раз

**Ожидаемый результат:** Ошибка или предупреждение

### Bug Test 2: Late Registration After Closure

**Сценарий:**
1. Установите `late_registration_ends_at` в прошлое
2. Попробуйте добавить игрока через позднюю регистрацию

**Ожидаемый результат:** Ошибка "Late registration is not available"

### Bug Test 3: Overlapping Seats

**Сценарий:**
1. Добавьте игрока на стол 1, место 5
2. Попробуйте добавить другого игрока на стол 1, место 5

**Ожидаемый результат:** Ошибка или автовыбор другого места

### Bug Test 4: No-Show Restore

**Сценарий:**
1. Отметьте игрока как `no_show`
2. Восстановите игрока
3. Проверьте что он может быть включен обратно

**Ожидаемый результат:** Статус изменен на `registered`

### Bug Test 5: Blocked User Registration

**Сценарий:**
1. Заблокируйте пользователя
2. Попробуйте зарегистрировать его на турнир

**Ожидаемый результат:** Ошибка 403

---

## ✅ 5. ФИНАЛЬНЫЙ ЧЕК-ЛИСТ ФУНКЦИОНАЛА

### Backend API (20 endpoints)

- [ ] POST `/api/games/:id/open-registration`
- [ ] POST `/api/games/:id/close-registration`
- [ ] POST `/api/games/:id/start-check-in`
- [ ] POST `/api/games/:id/check-in-player`
- [ ] POST `/api/games/:id/confirm-payment`
- [ ] POST `/api/games/:id/mark-no-show`
- [ ] POST `/api/games/:id/restore-player`
- [ ] POST `/api/games/:id/onsite-registration`
- [ ] POST `/api/games/:id/late-registration`
- [ ] POST `/api/games/:id/exclude-no-show`
- [ ] GET `/api/games/:id/stats`
- [ ] GET `/api/games/:id/players?status=`
- [ ] GET `/api/games/:id/late-registration/status`
- [ ] POST `/api/games/:id/finalize-results`
- [ ] GET `/api/games/:id/actions`
- [ ] GET `/api/games/:id/payments`
- [ ] GET `/api/games/:id/point-structure`
- [ ] POST `/api/games/:id/point-structure`

### Frontend Components

- [ ] AdminCheckInView отображается корректно
- [ ] AdminLateRegistrationView работает
- [ ] AdminTournamentManagementView показывает новые кнопки
- [ ] Статистика турнира отображается
- [ ] Поиск игроков работает
- [ ] Фильтры по статусу работают

### Database

- [ ] 3 новые таблицы созданы
- [ ] Новые поля в `games` работают
- [ ] Новые поля в `game_registrations` работают
- [ ] Триггеры работают корректно
- [ ] Функции PL/pgSQL работают

### Automation

- [ ] 8 cron jobs запущены
- [ ] Логи cron jobs выводятся
- [ ] Автоматизация работает корректно

### User Experience

- [ ] UI/UX понятен администратору
- [ ] Все кнопки работают
- [ ] Нет багов при навигации
- [ ] Toast-уведомления отображаются
- [ ] Ошибки обрабатываются корректно

---

## 📊 ОЖИДАЕМЫЕ РЕЗУЛЬТАТЫ

После полного прохождения тестов:

✅ **В базе данных:**
- 1 турнир со статусом `completed`
- 10-15 регистраций различных статусов
- 5-10 платежей в `tournament_payments`
- 50+ записей в `tournament_actions_log`
- Обновленная статистика в `user_stats`
- Корректная структура очков

✅ **В приложении:**
- Все игроки видят обновленный рейтинг
- История турнира отображается
- Администратор видит полный лог действий
- Платежи корректно записаны

✅ **Автоматизация:**
- Cron jobs работают без ошибок
- Логи выводятся корректно
- Уведомления отправляются (если настроены)

---

## 🚨 КРИТИЧЕСКИЕ ПРОВЕРКИ

Обязательно протестируйте:

1. ❗ **Безопасность:** Только админы могут управлять турниром
2. ❗ **Блокировка:** Заблокированные пользователи не могут участвовать
3. ❗ **Целостность данных:** Очки начисляются корректно
4. ❗ **Транзакции:** Платежи не дублируются
5. ❗ **Рассадка:** Нет конфликтов мест

---

## 📝 ОТЧЕТ О ТЕСТИРОВАНИИ

После завершения тестирования заполните:

```
Дата тестирования: __________
Тестировщик: __________

✅ Пройдено тестов: ___/50
❌ Провалено тестов: ___
🐛 Найдено багов: ___

Критические баги:
1. ________________
2. ________________

Некритические баги:
1. ________________
2. ________________

Предложения по улучшению:
1. ________________
2. ________________
```

---

## 🎉 ГОТОВО!

Если все тесты пройдены успешно - **система полностью готова к использованию!**

🎰 Удачных турниров! ♠️♥️♣️♦️


