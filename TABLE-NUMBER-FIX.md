# ✅ Исправление ошибки: колонки table_number и seat_number не существуют

## 🐛 Проблема

**Ошибка:**
```
Ошибка при начале турнира: column "table_number" of relation "game_registrations" does not exist
```

**Симптомы:**
- Администратор отметил всех, кто оплатил
- Исключил тех, кто не оплатил
- Нажал "Начать турнир"
- Появилась ошибка о несуществующих колонках `table_number` и `seat_number`

---

## 🔍 Причина

Методы пытались обновить или вставить колонки `table_number` и `seat_number` в таблицу `game_registrations`, но эти колонки не существуют в базе данных.

**Важно:** Данные о столе и месте игрока хранятся в таблице `table_assignments`, а не в `game_registrations`!

---

## ✅ Исправления

### 1. Метод `startTournament()` - убраны колонки из UPDATE

**Файл:** `server/models/Game.js`

**Было:**
```javascript
await query(
  `UPDATE game_registrations 
   SET status = 'playing', table_number = $1, seat_number = $2
   WHERE game_id = $3 AND user_id = $4`,
  [tableNumber, seatNumber, gameId, player.user_id]
);
```

**Стало:**
```javascript
// ВАЖНО: table_number и seat_number хранятся в table_assignments, а не в game_registrations
await query(
  `UPDATE game_registrations 
   SET status = 'playing'
   WHERE game_id = $1 AND user_id = $2`,
  [gameId, player.user_id]
);
```

**Результат:**
- ✅ Обновляется только статус на 'playing'
- ✅ Данные о столе и месте сохраняются в `table_assignments` (через INSERT выше)
- ✅ Не пытается обновить несуществующие колонки

---

### 2. Метод `lateRegistration()` - убраны колонки из INSERT/UPDATE

**Было:**
```javascript
const result = await query(
  `INSERT INTO game_registrations 
   (game_id, user_id, status, registration_type, is_late_entry,
    table_number, seat_number)
   VALUES ($1, $2, 'paid', 'late', true, $3, $4)
   ON CONFLICT (game_id, user_id) DO UPDATE
   SET status = 'paid',
       is_late_entry = true,
       table_number = $3,
       seat_number = $4
   RETURNING *`,
  [gameId, userId, seat.table_number, seat.seat_number]
);
```

**Стало:**
```javascript
// ВАЖНО: table_number и seat_number хранятся в table_assignments, а не в game_registrations
const result = await query(
  `INSERT INTO game_registrations 
   (game_id, user_id, status, registration_type, is_late_entry)
   VALUES ($1, $2, 'paid', 'late', true)
   ON CONFLICT (game_id, user_id) DO UPDATE
   SET status = 'paid',
       is_late_entry = true
   RETURNING *`,
  [gameId, userId]
);
```

**Результат:**
- ✅ Создается регистрация без попытки вставить `table_number` и `seat_number`
- ✅ Данные о столе и месте сохраняются в `table_assignments` через метод `assignSeatToPlayer()`
- ✅ Не пытается вставить несуществующие колонки

---

## 📊 Структура данных

### Таблица `game_registrations`:
- ✅ `id` - ID регистрации
- ✅ `game_id` - ID турнира
- ✅ `user_id` - ID игрока
- ✅ `status` - статус (registered/paid/playing/no_show/participated)
- ✅ `registration_type` - тип регистрации (online/onsite/late)
- ✅ `is_late_entry` - поздняя регистрация
- ✅ `registered_at` - время регистрации
- ❌ **НЕТ** `table_number` и `seat_number`

### Таблица `table_assignments`:
- ✅ `id` - ID назначения
- ✅ `game_id` - ID турнира
- ✅ `user_id` - ID игрока
- ✅ `table_number` - номер стола
- ✅ `seat_number` - номер места
- ✅ `created_at` - время назначения

**Вывод:** Данные о столе и месте хранятся **только** в `table_assignments`!

---

## 🧪 Тестирование

### Начало турнира:

1. ✅ Откройте панель управления турнирами
2. ✅ Найдите турнир со статусом "Ожидание"
3. ✅ Убедитесь, что есть оплатившие игроки (статус 'paid')
4. ✅ Нажмите "Начать турнир"

**Ожидаемый результат:**
- ✅ Турнир успешно начинается
- ✅ Статус меняется на "В процессе"
- ✅ Рассадка генерируется и сохраняется в `table_assignments`
- ✅ Статус регистраций меняется на 'playing' в `game_registrations`
- ✅ Игроки видят свои столы и места
- ✅ **НЕТ ошибки** `column "table_number" does not exist`

### Поздняя регистрация:

1. ✅ Начните турнир
2. ✅ Откройте панель приема
3. ✅ Найдите игрока и зарегистрируйте его с оплатой

**Ожидаемый результат:**
- ✅ Игрок успешно регистрируется
- ✅ Автоматически назначается свободное место
- ✅ Данные сохраняются в `table_assignments`
- ✅ **НЕТ ошибки** при создании регистрации

---

## 📁 Измененные файлы

1. ✅ `server/models/Game.js`
   - `startTournament()` - убраны `table_number` и `seat_number` из UPDATE
   - `lateRegistration()` - убраны `table_number` и `seat_number` из INSERT/UPDATE

---

## 🎯 Статус: ГОТОВО!

✅ Ошибка `column "table_number" does not exist` исправлена  
✅ Турнир успешно начинается  
✅ Рассадка генерируется и сохраняется в правильной таблице  
✅ Поздняя регистрация работает корректно  
✅ Данные о столе и месте хранятся в `table_assignments`  

---

## 📝 Примечания

- Данные о столе и месте игрока всегда должны браться из таблицы `table_assignments`
- При запросе рассадки нужно делать JOIN между `game_registrations` и `table_assignments`
- Это правильная архитектура: разделение данных о регистрации и рассадке

---

**После деплоя администратор сможет успешно начинать турниры! 🎉**

